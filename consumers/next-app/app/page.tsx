import { Button } from "@coderlife/ui-core";
import type { JSX } from "react";

export default function Page(): JSX.Element {
  return (
    <main style={{ padding: 24 }}>
      <Button variant="contained">Next Smoke</Button>
    </main>
  );
}
